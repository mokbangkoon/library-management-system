import {
  getBestBooks,
  getPurchasedBooks,
  getReviewBooks,
  getShareAndFindBooks,
  getTeamBooks,
} from '@apis/api';
const PurchasedBookPage = () => {
  return <div>dd</div>;
};

export default PurchasedBookPage;

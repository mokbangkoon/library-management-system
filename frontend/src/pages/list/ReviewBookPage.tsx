import {
  getBestBooks,
  getPurchasedBooks,
  getReviewBooks,
  getShareAndFindBooks,
  getTeamBooks,
} from '@apis/api';
const ReviewBookPage = () => {
  return <div>dd</div>;
};

export default ReviewBookPage;

import {
  getBestBooks,
  getPurchasedBooks,
  getReviewBooks,
  getShareAndFindBooks,
  getTeamBooks,
} from '@apis/api';
const FindBookPage = () => {
  return <div>dd</div>;
};

export default FindBookPage;

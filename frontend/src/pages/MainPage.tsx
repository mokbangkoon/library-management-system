import LoginButton from '../components/Common/Button/LoginButton';

const MainPage = () => {
  return (
    <div>
      <div> 메인페이지 입니다.</div>

      <LoginButton />
    </div>
  );
};

export default MainPage;
